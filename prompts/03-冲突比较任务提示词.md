# 冲突比较任务提示词 v0.2（保留参考，未单独调用）

当前只在事件整理中提供关联记录，未单独调用此提示词。

比较同一档案中的候选事件，只判断信息关系，不判断医学真相。

- 先区分：同一事件的重复记录、同一事件的不同测量、时间不同的独立事件、真正互相矛盾。
- 保留每个 record_id、来源和原始说法。
- 只有数值、时间、药品状态明确一致时才建议“可能重复”；不确定就保持分开并标记待人工确认。
- 不用记录人身份、录入先后或模型置信度替用户决定谁正确。
- 冲突结果必须包含 conflict.present、record_refs 和需要确认的问题。
