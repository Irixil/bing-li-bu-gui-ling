# 集成交接包 · 2026-09-13

这是 2026-09-13 的集成工作快照，供下一位同事直接拉取后继续。该快照位于独立分支 `codex/handoff-2026-09-13`，不改动 `main`。

## 当前已包含

- P0 前后端同源演示入口：`python -m scripts.start_demo`，默认端口 `18768`。
- 文字记录、整理、核对、历史、修订和就诊交接材料流程。
- SQLite 持久化、刷新/重启恢复、幂等提交和失败时原文保留。
- 媒体上传分片、原件保存、原件读取、识别状态、重试和 Event 关联。
- 录音暂停/续录状态处理，以及前端失败状态展示。
- 安全字段、危险提醒、来源和原文证据保留。
- 合同、前端可见性、录音、媒体和整理失败回归测试。

## 新鲜验证证据

本次打包前重新执行：

- `.venv312/bin/python -m pytest -q`：728 passed。
- `node --test tests/frontend_visibility.test.cjs tests/frontend_recording.test.cjs tests/frontend_media.test.cjs tests/frontend_organize.test.cjs`：16/16 passed。
- `npm run check:contracts`：通过。
- `node --check frontend/app.js`、`node --check frontend/media.js`、`git diff --check`：通过。
- 本机浏览器实际验证：打开首页、查看全部、打开记录详情、输入并保存文字记录；新记录出现在列表顶部。

## 明确未完成

- `online_passed=false`：真实模型、真实 ASR/OCR 不能视为已验收。
- 浏览器真实麦克风/相机权限、手机端暂停续录、真实媒体播放仍待实机验证。
- 真实模型账号、额度、模型托管资格和接口稳定性仍需接手人自行验证。
- 当前 `.env.example` 中的 DashScope/OpenAI-compatible 默认值只是配置模板，不能当作在线成功证据；不要把密钥提交到 Git。
- 现有 `docs/evidence/integration-2026-09-13.md` 和 `docs/evidence/selftest-2026-09-13.json` 是证据记录，需结合上述限制阅读。

## 接手步骤

```bash
git clone --branch codex/handoff-2026-09-13 https://github.com/Irixil/bing-li-bu-gui-ling.git
cd bing-li-bu-gui-ling
python3.12 -m venv .venv312
source .venv312/bin/activate
pip install -r requirements-dev.txt
npm install
python -m pytest -q
node --test tests/frontend_visibility.test.cjs tests/frontend_recording.test.cjs tests/frontend_media.test.cjs tests/frontend_organize.test.cjs
npm run check:contracts
python -m scripts.start_demo --check-config
```

在线运行前：复制 `.env.example` 为 `.env`，只在本机填写真实凭据；先读 `AGENTS.md`、`PROJECT.md`、`docs/team/README.md`、`docs/DEMO-SELFTEST.md`、`docs/evidence/integration-2026-09-13.md`，再按 `docs/API.md` 做逐步验收。不要在 `main` 上直接开发，不要提交 `.env`、token、runtime 数据库、媒体原件或虚拟环境。

## 分支和日期标记

- 分支：`codex/handoff-2026-09-13`
- 日期标签：`integration-2026-09-13`
- 基线主干：`main`（本次未修改）
