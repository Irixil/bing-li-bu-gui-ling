# B 媒体识别证据

状态：**真实服务未验证**。

当前环境没有已确认授权的 ASR/OCR 服务凭据、服务样例许可或可公开的脱敏录音／照片，因此本目录不放真实医疗媒体、不放 `.env`、不放供应商响应，也不把 Mock 结果当成真实成功证据。

当前可复现的软件证据：

- `tests/test_recognition.py` 的离线测试覆盖统一识别接口、显式 Mock、原件只读、格式和资源边界、失败分类、非法响应和敏感信息脱敏。
- 全量回归当前为 `220 passed`；其中识别模块专项为 17 项。

补齐 B1/B2 前必须由负责人确认：

1. 供应商、模型、配置变量和数据传输授权；
2. 一段获准使用的中文短录音和一张获准使用的清晰印刷资料照片；
3. 发送前的文件大小、格式、时长／像素检查；
4. 原话与识别结果的人工对照，特别记录错字、遗漏、药名、数值、单位和日期；
5. 成功、超时、鉴权失败和服务不可用的脱敏响应；
6. 实测耗时、费用和重试／重复计费边界。

在这些证据具备前，发布文案必须写“识别模块和 Mock 接线已完成，真实 ASR/OCR 未验证”。
