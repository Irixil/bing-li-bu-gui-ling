# 真实在线 HTTP 自测 · 2026-09-13

## 环境

- 分支：`codex/online-acceptance-2026-09-13`
- 服务：本机 `http://127.0.0.1:18769`，独立临时 SQLite 与媒体目录
- 输入：仓库合成资料 `data/synthetic/selftest/voice.wav`、`card.png`；不含真实患者信息
- 凭据：仅从本机环境变量注入，证据不记录 token、`.env`、数据库或媒体原件

## 真实 provider

- 文字整理与修订：OpenAI-compatible，模型 `glm-5.3-flash`
- ASR：OpenAI-compatible 音频转写，模型 `glm-asr-2512`
- OCR：OpenAI-compatible 视觉对话，模型 `glm-4.6v`
- 三类结果均记录为 `is_mock=false`

## 结果

- 全链路自测：`156/156 passed`，`passed=true`，`online_passed=true`，64 次 HTTP 调用
- 覆盖：文字保存/幂等/整理/核对/历史/修订/交接；音频和图片分片上传、原件字节校验、真实识别、关键词、关联记录和交接附件
- 真实 ASR 输出命中“胸口疼”“喘不上气”；真实 OCR 输出命中同样关键词；危险提醒在整理、核对、交接中持续保留

## 修复记录

- 首次真实整理时，模型将安全说明错误放入 `forbidden_actions`，后端按安全契约返回 422 并保留原文；提示词补充该字段必须省略或为空数组，版本升至 `prompt-v0.6`，修复后真实整理成功。
- 用户详情页原先把整个草稿 JSON 直接展示；现改为中文卡片（记录类型、时间、核对角色、关注级别、原话、待核对问题），并增加前端回归测试。

## 边界

- 浏览器真实麦克风录音已点击验证暂停计时、续录和原件保存；浏览器生成 WebM 的一次真实 ASR 请求返回协议错误，页面明确显示失败且原件仍可查看，未伪造成功。仓库 WAV 真实 ASR 已通过。
- 手机真机、真实媒体播放和通用临床准确率仍未验收。
