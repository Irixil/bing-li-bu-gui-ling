# 最小闭环浏览器验证（2026-09-18）

## 检查对象

- 前端：`http://127.0.0.1:5174/?build=minimal-loop-20260918-2`
- 后端：`http://127.0.0.1:18769/`
- 模型：Mock，本次不产生付费请求，不用 Mock 证明真实 OCR 质量
- 数据：独立浏览器中新建的测试加密仓库和仓库自带吉祥物图片，不使用真实健康资料

## 走过的路径

1. 在全新来源上建立本机加密仓库，页面只要求本机恢复口令。
2. 进入照片页，选择测试 PNG，点击“保存并识别”。
3. 记录页面 `confirm` 调用次数，结果为 `0`；没有出现 AI 外发确认、家属绑定或在线开通提示。
4. 原件状态变为“已保存”，随后自动完成 Mock OCR。
5. 识别结果自动建立记录；看病资料页显示 `1` 条记录、`1` 张资料照片，媒体显示“文字已识别，待核对 · 已关联记录”。
6. 进入“我的记录”，点击“生成就诊交接材料”。
7. 材料成功包含识别原文、记录状态、照片原件保存状态、文字识别状态和记录关联状态，并提供打印入口。

## 现场发现并修复

首次选图使用了浏览器无法持续读取的临时路径，暴露了“预览后临时文件句柄可能失效”的边界。前端已改为选择后立即用 `FileReader` 复制字节到内存 Blob，不再依赖后续的临时文件句柄。换用浏览器可读取的临时测试文件后，完整闭环通过。

## 自动化结果

- Python：`770 passed`
- Node：`38 passed`
- TypeScript 合同检查：通过
- Impeccable 机械检查：降级为正则扫描（本机缺少 HTML 解析模块），返回 `[]`；该结果不代替真实浏览器验收

## 结论

当前代码已跑通“保存原件 → 无弹窗自动识别 → 自动建立记录 → 读取 → 生成就诊交接材料”的最小闭环。真实 AIHubMix 连通性沿用既有受控合成样例证据；本次没有新的付费模型请求，不声称真实手机照片或 WebM 识别质量已验收。
