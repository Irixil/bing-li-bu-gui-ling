# 完整前端与本地双服务复核（2026-09-17）

## 问题与修复

- 前一次 `5173` 入口虽然能打开，但仓库内只合入了简化前端，没有调用另一工作目录中已完成的完整 elder-ui。
- 本次把原有吉祥物、启动页、语音优先首页、照片入口、记录时间线、看病资料、底部导航和移动端样式合入当前仓库。
- 保留当前内测版的设备端加密仓库、显式 AI 外发同意、加密备份、时间线筛选、修订/删除/交接和反馈功能。
- 发现旧 Service Worker 会让浏览器继续读旧前端资源；已改为网络优先、升级缓存版本，并给当前静态资源加版本参数。

## 真实浏览器证据

- `http://127.0.0.1:5173/` 标题为“病历不归零·内测版｜陪你记下来”，页面包含“身体感觉怎么样？”首页、“我的记录”和“看病资料”三个底部入口。
- 首次使用显示与主页同一品牌的设备加密引导，不再看起来像后端管理页。
- 在独立浏览器原点建立加密仓库，从语音页面直接保存一条文字记录；刷新、重新解锁后，记录仍可在“我的记录”中找到。
- 浏览器结果：`active=recordsView`、`mode=本机加密保存`、`persisted=true`、`overflow=false`。

## 自动化与双端口证据

- JavaScript 前端、加密仓库、媒体和安全规则：`36 passed`。
- Python 后端及 HTTP 回归：`765 passed`。
- TypeScript 前后端合同：通过。
- 配置检查：DeepSeek 文字整理、AIHubMix ASR、AIHubMix OCR、本地服务、网站访问保护均已配置。
- 从 `Origin: http://127.0.0.1:5173` 调用 `http://127.0.0.1:18768`：健康检查返回 `200`，登录返回 `200`，后续会话返回 `authenticated=true`。本次没有为了配置复核再触发付费模型请求。

## 结论

当前本地产品入口是完整前端 `http://127.0.0.1:5173/`，后端 `http://127.0.0.1:18768/` 只是 API。两个服务已独立运行并完成本机记录持久化与跨端口访问保护闭环。正式云端部署、真实手机媒体和私有 TOS 恢复仍需后续独立验收。
