# 设备绑定与单口令浏览器验证（2026-09-17）

验证对象：本地工作树，前端 `127.0.0.1:5174`、后端 `127.0.0.1:18769` 的隔离测试服务；正式本地入口继续为 `5173` / `18768`。

## 自动化回归

- Python 全量：`770 passed`。
- Node 前端：`37 passed`。
- TypeScript 前后端合同：通过。
- 设备绑定专项：短时令牌签名、篡改、过期、30 天持久 Cookie、精确来源、CSRF、生产默认关闭自动绑定、回环地址严格自动绑定全部通过。
- 前端合同：不存在 `cloudPassword`、`cloudLoginForm` 或老人端 `/api/app/login` 调用；存在设备激活和绑定失效非阻塞提示。

## 隔离真实浏览器

1. 使用全新前端来源打开完整产品页面，首次只设置本机恢复口令。
2. 建立仓库后直接进入语音优先首页，没有出现网站访问密码弹窗。
3. 保存合成文字“浏览器设备绑定回归：今天早上散步后有点累，现在已经休息。”，页面显示“文字原话 · 已加密保存在本机”。
4. 后端 `/api/app/session` 返回 `200` 且 `authenticated: true`；页面 DOM 中 `cloudLoginGate`、`cloudPassword` 均不存在。
5. 刷新页面后只出现本机恢复口令；解锁后合成记录仍在，设备会话仍为已认证，没有第二密码弹窗。
6. 设置页显示“在线功能已由家属开通，这台设备可以直接使用 AI 和云备份”，并同时说明本机记录不依赖在线开通。

本验证没有调用付费 AI、没有上传真实健康资料、没有接触 `5173` 来源中已有的用户加密数据。隔离浏览器任务空间完成后已清理。

界面规则检测以降级正则模式执行（本机缺少可选 HTML 解析模块），未发现规则命中；因此该结果只作为辅助证据，不替代上述真实浏览器操作和前端合同测试。
