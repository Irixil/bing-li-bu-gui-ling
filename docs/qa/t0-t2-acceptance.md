# 任务 D：T0–T2 阶段验收摘要

证据基线：`main@6e1a7f3f5d33b66b259fb2e35784e9447be23a07`；当前 D 工作分支：`codex/task-d-qa`
适用范围：公开病例资料包、现有文字后端闭环和可执行的后续测试准备。
不适用范围：媒体实现、前端页面、真实识别、手机验收和发布决定。

当前判断：T0–T2 已形成可用、可复核的准备，但由于 QA 收尾项和最终集成复测尚未完成，不能称为完整验收。本文的“已交付”仅限对应资料或文字范围。

## 已交付

- 公开病例登记表已建立：1 位公开论文患者、3 段同一病例摘要；来源、CC BY 4.0 许可依据、改编/脱敏范围、`source_kind`、未知发生时间 `null`、预期行为、限制和 `CASE-*`/`TEXT-*` 映射齐全。最终许可和对外使用范围由项目负责人或相应责任人确认。见 [registry.json](../../data/public_cases/registry.json)。
- 测试矩阵已建立，覆盖 T0–T6 编号规则；当前文字闭环用例包含保存、危险提醒、未命中边界、422 保留原文、核对、修订、幂等、版本冲突、交接、Schema 和本地会话边界。见 [test-matrix.md](test-matrix.md)。
- 缺陷模板、当前缺陷登记和证据模板已建立。环境缺依赖单独记录为 `ENV-20260912-001`，没有把它误记为后端业务缺陷。见 [defect-template.md](defect-template.md)、[defects.md](defects.md)、[evidence-template.md](evidence-template.md)。
- 用户已确认当前角色为任务 D：负责公开病例、测试和质量验收；D 检查合同、医学/法律/产品和发布依据并提出建议，不负责冻结合同、作最终决定、开发前端或合并发布。
- 在临时 Python 3.12 隔离环境安装声明依赖后，当前提交重跑得到：后端测试 `71 passed`、合成 Mock 结构校验 `40/40`、指定危险样例安全断言 `6/6`、公开病例流程 `1 patient / 3 extracts`，数据库重开和进程停止/重启检查通过。逐项证据见 [text-baseline-2026-09-12.json](../evidence/text-baseline-2026-09-12.json) 与 [process-restart-2026-09-12.json](../evidence/process-restart-2026-09-12.json)。
- 整理失败后的重试已实际执行：第一次返回 422 后，原文、`record_id` 和 Event 保留；再次整理返回 200，Event 数量仍为 1，审计记录包含失败和成功两次尝试。见 [text-retry-2026-09-12.json](../evidence/text-retry-2026-09-12.json)。

## 条件式交付

- 系统默认解释器缺少 `pytest` 和 `jsonschema`，直接运行命令会阻塞；可按 `requirements-dev.txt` 建立临时隔离环境重跑。该环境事实和重跑结果均已记录，不能把缺依赖隐藏成“无问题”。
- 进程重启和文字数据库备份已有历史/隔离环境证据，但媒体原件备份恢复尚未实现，因此不能扩展为“媒体恢复通过”。
- Mock 的 `40/40` 只表示 40 条合成样例结构校验通过；指定危险样例安全断言为 `6/6`。这些结果不表示 40 条业务语义全部正确，也不表示真实模型质量或临床安全。
- 当前 QA 资料状态和证据已同步，病例引用动态校验也已通过；仍需在集成其他分支后的最终基线统一复测。

## 等待负责人决定

- 媒体合同、真实 ASR/OCR 服务、费用/数据外传、录音暂停参数和受控手机环境，待项目负责人/集成人确认后再进入 T3–T6。D 负责检查确认是否存在并提出验收建议，不代替负责人冻结合同或作最终产品/法律/医学决定。
- 比赛演示范围、Mock/真实服务的对外说法和是否发布，待项目负责人/集成人根据 D 的证据与建议确认。

## 明确不在本次范围

- 文件上传、媒体原件存取、媒体状态、ASR/OCR、媒体备份恢复。
- 完整老人端网页、浏览器/触控/麦克风/相机权限和录音续录体验。
- 真实老人/医生效果、临床安全、敏感度/特异度、付费意愿和公网生产安全。

## 尚未完成

- 在最终集成提交上重跑 T0–T2 文字、病例、重试和证据检查。
- 等媒体合同/实现、前端提交和受控设备就绪后，执行 T3–T6 验收。

## 阶段结论

T0–T2 的“可用准备”已具备，可在声明依赖环境下复核；因上述 QA 收尾项和最终集成复测尚未完成，当前不构成完整验收，也不构成媒体、前端或真实识别功能的完成声明。进入 T3 前仍需媒体合同和实现提交，T5 还需前端提交与受控设备；D 将提供验收和发布门禁建议，最终决定由项目负责人/集成人确认。
