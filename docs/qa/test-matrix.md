# 任务 D 测试矩阵

基线提交：`6e1a7f3f5d33b66b259fb2e35784e9447be23a07`。本表先固化可复现步骤和范围；媒体、前端、手机及发布验收在对应功能和合同完成后再执行。

## 结果状态

本表使用 [QA 资料状态定义](README.md#状态含义)。`已交付` 只表示该行已有当前提交证据，不代表整个比赛产品完成。

## T0–T2：公开病例与文字闭环

| 用例 ID | 阶段 | 输入/前置 | 操作或命令 | 预期 | 当前实际/证据 | 状态 | 缺陷号 |
|---|---|---|---|---|---|---|---|
| `CASE-PUBLIC-01` | T1 | `PMC12890330-01` | 按 `scripts.demo` 导入并核对 | 1 位公开患者、来源为 `document`、发生时间保留 `null`；胸闷/呼吸困难命中本地提醒 | 当前隔离环境导入成功；登记见 `data/public_cases/registry.json`，运行证据见 `docs/evidence/text-baseline-2026-09-12.json` | 已交付 | — |
| `CASE-PUBLIC-02` | T1 | `PMC12890330-02` | 同上 | 心率 40 未命中当前规则时不得写成正常；原文、来源和未知时间保留 | 当前隔离环境导入成功；限制已登记并与证据逐项对应 | 已交付 | — |
| `CASE-PUBLIC-03` | T1 | `PMC12890330-03` | 同上 | 未命中规则不等于医学正常；不补写绝对日期 | 当前隔离环境导入成功；限制已登记并与证据逐项对应 | 已交付 | — |
| `TEXT-SAVE-01` | T2 | 普通合成文字 | `<venv>/bin/python -m pytest -q` 中保存/状态相关测试（依赖见 `requirements-dev.txt`） | 原文先保存，响应状态为 `inbox`，原文可查询 | 隔离依赖环境 `71 passed`；见 `docs/evidence/text-baseline-2026-09-12.json` | 已交付 | — |
| `TEXT-DANGER-01` | T2 | 胸闷、气短公开摘要 | 保存后整理、核对、生成交接 | 本地危险提醒出现，核对后仍在交接未解决项 | 公开病例行 01 与 handoff 断言通过 | 已交付 | — |
| `TEXT-DANGER-02` | T2 | 心率 40 摘要 | 保存并检查 API 返回与登记限制 | API 结果只表示当前规则未命中，保留“不能解释为医学正常”的限制 | 当前文本流程通过；用户可见 UI 文案尚未测试 | 已交付 | — |
| `TEXT-DANGER-03` | T2 | 食欲下降、腹痛、腹泻及全身无力公开摘要 | 保存并检查 API 返回与登记限制 | 只表示当前规则未命中；不显示“无危险”或“正常”，不补写症状起始日期 | 当前文本流程通过；原文、来源和未知发生时间保留；见 `PMC12890330-03` 登记与文字基线证据 | 已交付 | — |
| `TEXT-422-01` | T2 | 断网、超时、非法 JSON、空对象/异常对象 | `<venv>/bin/python -m pytest -q` 的离线安全测试 | 返回 422 时原文和危险状态保留，失败类型可解释，不把失败当作成功 | 隔离依赖环境相关测试通过；重试动作另列 `TEXT-RETRY-01` | 已交付 | — |
| `TEXT-RETRY-01` | T2 | 整理失败后的可重试接口 | 先让 organize 失败返回 422；再以原 `expected_version` 重试，确认原文和 `record_id` 不变、Event 总数不增加，并在审计中同时看到 `organize_failed` 与 `organized` | 聚焦 HTTP 验收通过：临时 SQLite 中第一次 422 后，同一 Event 以 version 1 重试返回 200，变为 `draft/version=2`；Event 总数保持 1；审计顺序为 `created → organize_failed → organized`；见 [text-retry-2026-09-12.json](../evidence/text-retry-2026-09-12.json) 和 `tests/test_task_d_retry.py` | 已交付 | 集成其他分支后在最终基线重跑 |
| `TEXT-REVIEW-01` | T2 | 危险文本已整理 | `review confirm` 后再查详情/交接 | 核对不清除危险提醒 | `danger_remains_after_record_confirmation=true` | 已交付 | — |
| `TEXT-REVISION-01` | T2 | 已记录文本 | 修订并查看旧版/新版本/历史 | 旧原文保留，新版本重新扫描，来源链不断 | 隔离依赖环境测试通过 | 已交付 | — |
| `TEXT-IDEMP-01` | T2 | 同一请求体和幂等键 | 重复 `POST /api/events`；再用同键不同内容 | 同键同内容不重复；同键不同内容返回 409 | 公开病例演示逐条检查通过 | 已交付 | — |
| `TEXT-VERSION-01` | T2 | 同一 `expected_version` 的并发/过期修改 | 重复核对或修订请求 | 版本冲突返回 409，不强制覆盖 | 隔离依赖环境测试通过 | 已交付 | — |
| `TEXT-RESTART-01` | T2 | 临时 SQLite + 公开病例 | 停止并重启服务，再查记录 | 文字原文、危险状态、版本和交接快照可恢复 | 当前提交重跑通过，见 `docs/evidence/process-restart-2026-09-12.json`；媒体重启另列 `MEDIA-RESTART-01` | 已交付 | — |
| `TEXT-BACKUP-01` | T2 | 本地临时数据库 | 备份并恢复到新目录 | 文字原文、安全状态和历史可读；不把文件缺失当成功 | 隔离依赖环境测试通过；媒体原件备份另列 `MEDIA-BACKUP-01` | 已交付 | — |
| `TEXT-HANDOFF-01` | T2 | 公开病例三条记录 | 生成并读取 handoff | 来源、待核对项和危险未解决原因保留；快照不被后续修订改变 | 公开病例演示断言通过 | 已交付 | — |
| `TEXT-SCHEMA-01` | T2 | 越权输出、错误来源/时间/引用 | 运行 adapter/schema/safety 测试 | 非法输出拒绝，时间和来源不凭空生成 | 隔离依赖环境相关测试通过 | 已交付 | — |
| `AUTH-BOUNDARY-01` | T2 | 本地会话/兼容账号测试夹具 | `<venv>/bin/python -m pytest -q` 的认证与隔离测试 | 当前本地合同边界内拒绝越权；不把本地单人会话宣称为公网多租户 | 隔离依赖环境相关测试通过；公网部署不在范围 | 已交付 | — |

## T3–T6：等待实现或负责人确认

| 用例 ID | 阶段 | 依赖 | 预期验收重点 | 当前状态 |
|---|---|---|---|---|
| `CONTRACT-MEDIA-01` | T3 | A/B 媒体合同冻结 | 字段、状态、错误码和重试语义与正式 API 一致 | 条件式交付：草案未冻结 |
| `MEDIA-UPLOAD-01` | T4 | 原件保存/API | 原件先保存，失败不显示已保存，重复请求不重复 | 条件式交付：功能尚未实现 |
| `MEDIA-RECOGNITION-01` | T4 | 真实 ASR/OCR 或明确 Mock | 机器初稿、失败原因、危险扫描与 Event 关联可追溯 | 条件式交付：真实识别未验证 |
| `MEDIA-RESTART-01` | T4 | 媒体状态与备份 | 重启后原件、状态、初稿和重试入口可找回 | 条件式交付：功能尚未实现 |
| `MEDIA-BACKUP-01` | T4 | 媒体原件备份实现 | 数据库与原件一并恢复，校验和相符 | 条件式交付：功能尚未实现 |
| `UI-FLOW-01` | T5 | 前端负责人提交 | 老人路径中的保存、处理中、失败、危险和核对提示准确 | 条件式交付：D 负责验收，前端实现尚未提交 |
| `UI-DANGER-02` | T5 | 前端文案与公开病例 02/03 | 未命中规则的页面不显示“无危险/正常” | 条件式交付：尚无前端验收 |
| `MOBILE-RECORD-01` | T5 | 受控手机、HTTPS、已确认参数 | 长时间无声先提醒，无回应再暂停；续录不丢前后内容；不恢复 60 秒限制 | 等待用户决定：设备和参数未确认 |
| `RELEASE-SCOPE-01` | T6 | 集成人与项目负责人确认 | 冻结可展示范围，明确 Mock/真实/公开/合成标识 | 等待用户决定：尚未进入发布验收 |

## 证据规则

- 每个已交付行必须有命令、提交号、环境、预期/实际和脱敏证据路径。
- 失败、缺依赖、未实现、合同未冻结和角色待确认分开记录，不用“通过”替代。
- 任何公开病例条目都必须引用 `data/public_cases/registry.json`；合成数据必须引用 `data/synthetic/`。
- 媒体和前端未实现前，不能用文字测试或 Mock 结果代替 `MEDIA-*`、`UI-*`、`MOBILE-*` 验收。
