# 验证记录

## 2026-09-14 后端选择性整合

当前分支从 `feaf182` 创建，只选择性纳入任务 D 的公开病例登记、QA 结构和失败后重试用例，并把复杂医学图片保留为 MVP 外 P1；没有用旧分支覆盖 `backend/recognition.py`，也没有修改 `frontend/**`。

本机 `python3.12 scripts/validate_task_d.py` 已通过，确认 3 条登记与运行夹具一致、20 个 QA 用例编号完整、P1 边界存在；`py_compile` 与 `git diff --check` 通过。这些是静态和资料检查，不等于运行回归。

用户确认后已在 `.venv312` 安装锁定依赖。任务 D、API、安全、备份和进程重启专项为 232 项通过；完整 Python 回归在干净提交 `16c56cad71e84b888320a7992752e8ac6d0f95e6` 上为 728 项通过、1 项因本机缺少可选 `ffmpeg` 跳过；新增重试专项单独复跑为 1 项通过。TypeScript 合同与前端脚本语法检查通过。公开病例 Mock 演示为 1 位患者、3 段改编资料通过。默认 40 条 Mock 只通过结构 40/40 和指定危险 6/6，仍有 54 项语义断言失败、146 项未自动评估；11 条 C 严格夹具通过 121 项自动断言，29 项需人工复核。当前不得把这些结果写成真实模型、临床或生产验证。详细盘点见 [本机资源记录](evidence/backend-local-inventory-2026-09-14.md) 和 [本轮证据](evidence/backend-integration-2026-09-14.md)。

目标版本：集成候选 `codex/integration-demo-version`，Python 3.12；文本整理使用 `MODEL_PROVIDER=mock`，媒体识别需显式设置 provider。报告文件在 `docs/evidence/`。合并后需在本候选上重新执行完整测试、Mock 评测和演示，并补充前端浏览器验收记录。

## C 与共享 main 整合验收（2026-09-12 最新）

已将 main `6e1a7f3` 合入 C 分支，实际验证代码提交为 `c268495`，Python 3.12.13 / macOS arm64。**480 passed**（21.67 秒）；旧 40 条结构／指定安全 smoke 通过；C 的 11 条严格 Mock 通过 121 项自动断言，29 项人工未判定；1 位公开患者的 3 段改编资料本地闭环通过；24 类临床清单盘点完成。旧 40 条的 54 个语义断言失败及 15 个标签问题仍保留，不能称语义全通过。

实际命令、退出码、日志与源文件哈希见 `docs/evidence/c-integration/`。本轮没有调用远程模型，医学签署仍 pending。后续交接与 CI 文档提交不改变这些受测源文件；最新 PR 提交的远端执行结果以 GitHub Checks 为准。本 PR 为 CI 增加同一条 C 严格 Mock 命令和清单盘点。合并步骤及与 B PR 的关系见 [HANDOFF-C.md](../HANDOFF-C.md)。

## C 规则草案与占位增补（2026-09-12 历史阶段）

最终全量 **480 passed**。11 条 C 合成用例在 Mock 下通过 121 项自动断言，仍保留 29 项人工待复核；公开病例 1 位患者的 3 段资料通过临时数据库的保存、整理、确认、历史、交接与重启闭环。此次不重新调用远程模型，之前 438 项及模型联调数据保留为历史证据。

新增 24 类医学待审清单；离线扫描 54 条测试材料，53 条匹配待查话题，这不是疾病或异常统计。独立对抗审查发现并修复无心率上下文、数字/单位截取，以及交接待办被记录确认清空的问题。临床规则为 AI 草案，医学来源和专业签署仍 pending；计时占位全部 null 且不在运行时导入。当前证据见 `docs/evidence/clinical-review/`，规则及上线前待补内容见 [临床规则草案](CLINICAL-RULE-DRAFT.md)。

## 通用模型接口修订（同日后续）

在 C 安全修复后增加通用中转站兼容层，最新全量 **438 passed**（包含此前绑定错误分类修复）。魔搭 `deepseek-ai/DeepSeek-V4-Pro-0813` 已返回通过应用校验的真实输出，包含合成单条和公开病例第 1 段；60 秒配置下也发生多次超时，不能称稳定性已通过。批次范围、机器辅助审核和剩余问题见 [MODEL-INTEGRATION.md](MODEL-INTEGRATION.md) 与 `docs/evidence/deepseek-v4-pro/`。下方 C 的 258 项和基线 71 项属于历史快照。

## C 部分本地修订（2026-09-12）

当前检查对象为 `codex/model-safety` 工作区；基线提交 `e1a5967`。第一性原理对抗审查、当前测试与剩余问题以 [C-REVIEW.md](C-REVIEW.md) 为准。新增报告在 `docs/evidence/c-review/`，原 `docs/evidence/` 文件保留为基线历史，不代表最新工作区。

下面是原基线交付时的验证记录。其“71 passed”和旧源码清单均属于基线。C 修订已补充真实断言：旧 40 例仍通过结构与指定安全 smoke，但严格检查暴露语义失败和旧标签问题，不能把原来的 smoke 结果理解为 40 条语义全部正确。

## B 分支增量验证

目标分支：`fix/backend-api`，当前验证提交 `36f3b67`（父提交 `89a2ee2`）；`origin/main` 的历史共同基线为 `e1a5967`。以下结果是 2026-09-12 在本地 Python 3.12 对当前实现提交重新执行的证据，不替代合并后发布候选上的重跑：

- `python -m pytest -q`：**220 passed**。在原 71 项文本底座基础上，加入后端 HTTP/并发/发布门槛回归、17 项识别模块测试和媒体上传/持久化/状态机/HTTP/备份恢复测试，覆盖健康检查/token、创建幂等与冲突、版本竞争、AI/识别失败保留、危险扫描、完整文字、Event 关联、交接快照、CORS/Origin、未预期异常脱敏、媒体能力发现、未配置边界禁写和真实后端进程重启恢复。JUnit 证据见 `docs/evidence/b-backend-tests.xml`。
- 新增测试以真实临时 HTTP 服务、临时 SQLite 和真实后端子进程为主要接缝；外部模型仍使用 Mock/失败适配器，不把供应商行为冒充成本地验证。TypeScript 调用者合同已通过 `npm ci && npm run check:contracts`。
- 另以临时数据库启动真实 HTTP 进程，实际跑通普通保存→整理→核对→历史→修订→交接、旧版本 `409`、重启后 SQLite 恢复与 token 轮换，以及 ModelScope 缺配置时原文/危险提醒仍保留的 `422`。数据库位于系统临时目录，未污染 `runtime/records.sqlite3`。
- 这 220 项与实机演练仍不证明真实浏览器页面、真实模型质量、临床安全或公网部署能力。A 的浏览器联调以及 C/E 在唯一发布候选上的证据仍是比赛冻结前的外部门槛。

## 媒体后端说明

2026-09-12 协作更新：媒体后端已在 `fix/backend-api@36f3b67` 接入本地文件存储、SQLite 状态、识别模块、危险扫描、Event 关联、HTTP 接口和备份恢复。以下证据仍只覆盖本地文件、Mock/替身和自动化测试；真实 ASR/OCR、浏览器、手机暂停续录和公网部署不能由这些结果推出。联合验收见 [团队任务包](team/README.md) 和 [合并指南](team/INTEGRATION.md)。

## 已通过

- `python -m pytest -q`：**71 passed**。覆盖保存先于 AI、断网/超时/非法 JSON/异常对象、普通输入、危险输入、模型漏标危险词、原文查询与 SQLite 重启恢复、幂等、修订、历史、交接、账号兼容、旧 Schema/来源/时间/越权校验、本地备份。
- `python -m backend.evaluate_mock`：40 条合成用例，Schema 40/40，指定安全用例 6/6。结果明确标记 synthetic/mock。
- `python -m scripts.demo`：1 位公开论文病例、3 段同一病例改编摘要。保存、幂等、整理、核对、详情、历史、交接、数据库重新打开均通过；第 1 段胸闷/气短命中危险提醒。结果在 `docs/evidence/public-case-demo.json`。
- 公开病例来源和 CC BY 4.0 署名在 `data/public_cases/README.md`；没有使用医院原始病历。
- 用 `python -m backend.run_local` 真正启动并停止进程，再重启同一个临时数据库；3 条公开病例记录的原文、危险状态、记录版本和交接快照恢复成功，见 `docs/evidence/process-restart.json`。

## 基线交付时尚未验证（历史，最新模型状态见文首）

- 前端由项目负责人在独立任务开发，尚未通过本项目完整真实浏览器操作验收。
- 真实手机录音格式、浏览器 Blob 播放、真实 ASR/OCR、媒体暂停续录合并和真实供应商失败路径尚未验收；本地自动化媒体证据不能代替这些外部证据。
- 新包没有接真实模型成功证据；Mock 不能证明模型质量。`backend.evaluate_modelscope` 在没有明确凭据时会拒绝运行。
- 没有真实老人、医生或护士参与观察；公开论文改编只验证软件流程，不能证明临床安全、敏感度、特异度或付费意愿。
- 只在当前 macOS 环境实际跑过；Windows/Linux 安装步骤按标准 Python 写出，但还没有现场验证。
- 公开病例中“心率每分钟40次”不在当前危险规则覆盖范围；未命中不等于正常。

## 运行边界

本项目服务只监听本机 127.0.0.1，默认一个本地数据库。旧兼容账号路径仍在，不能直接当公网多人系统。危险规则是关键词兜底，否定、历史、隐喻可能误报，未覆盖表达可能漏报；它不诊断、不改药。

## 发布前自查结论

这是开发仓库分享，不是公网医疗服务上线。

- 核心后端路径与重要失败路径：在上述本机/Mock 范围内通过。明天整个网页 demo：尚未验证，需前端完成后排练。
- 改动代码：原文落库先于模型调用，模型异常捕获不包住数据库提交，失败写审计，旧原文和危险历史不被覆盖。保留来源、时间及安全校验；备份使用 SQLite backup API。
- 依赖版本固定；仅使用 Python 标准库和 jsonschema 运行后端。测试依赖单独列出。未执行第三方漏洞数据库扫描，不能据此声称没有已知漏洞。
- 没有将旧 `.env`、运行数据库、依赖目录或旧前端复制到本仓库。提交前将检查实际 Git 暂存清单。
- 原始测试凭据是单测里公开的虚构字符串，不是用户真实凭据。自动化/AI 自查不代替独立临床或安全审查。

基线代码和数据文件的 SHA-256 清单见 `docs/evidence/source-manifest.json`；B 分支增量对象见 `docs/evidence/b-source-manifest.json`。它们仅定位验证对象，不声称已完成正式产品验收。
