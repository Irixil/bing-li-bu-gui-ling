# 问题清单

> 由 `.dz/state.json` 生成。DZ 判断问题该放到哪里；改变产品约定前仍需用户确认。

| ID | 问题 | 分类 | 去向 | 当前情况 | 影响 | 关联工作 | 验证证据 | 防止再犯 |
|---|---|---|---|---|---|---|---|---|
| MVP-HANDOFF | 后端已验证，整个比赛网页仍需前端与真实模型联调 | 动手办法需要调整 | 调整动手办法 | 确认不用处理 | 不能把后端通过说成整个比赛demo或临床产品已完成；下一步由用户负责前端，团队按docs/PROJECT-PLAN.md接线与排练 | — | — | — |
| MEDIA-TEAM | 两人媒体后端任务包已编写，真实功能待实施与联合验收 | 动手办法需要调整 | 调整动手办法 | 确认不用处理 | 实施前需对齐 HTTP 和识别函数合同、服务能力、限制及权限；不能把文档发布或旧文本证据当作媒体已完成 | — | — | — |
| BACKEND-INTEGRATION-ENV | 当前后端 worktree 缺少离线回归依赖 | 动手办法需要调整 | 调整动手办法 | 确认不用处理 | 静态校验可以继续，但新增测试和完整后端回归不能声称通过 | — | — | — |
| P1-COMPLEX-IMAGE | 复杂医学图片识别保留为 MVP 外待办 | 以后可能再加的想法 | 放到以后 | 留到以后 | 后续若启动需重新决定服务、资料外传、人工核对、证据和接口合同 | — | — | — |
| ASR-WEBM-REAL | 浏览器真实 WebM 识别仍有协议错误 | 原来说好的没做到 | 当前修理任务 | 已经分好去处 | 不能宣称浏览器录音通用真实识别完成；本后端整合不修改 frontend 或伪造成功 | — | — | WAV 通过后再用浏览器实际 WebM 走完上传、识别、危险扫描与 Event 关联；不用合成 WAV 代替 WebM 验收。 |
| MEDIA-REAL-CREDENTIALS | 真实 ASR 和 OCR 缺少本地服务凭据 | 动手办法需要调整 | 调整动手办法 | 已经分好去处 | 离线完整 Demo 可运行，真实文字可运行，但不能完成真实录音转写、图片识字或宣称全真实在线 Demo 跑通 | — | — | 启动前同时核对 Key 的 /v1/models 可用列表和公开模型页；首次只发合成资料，不自动重试或切换未验证模型。 |
| SPLIT-ENTRY-20260916 | 使用者入口被错误设计成同源后端地址 | 原来说好的没做到 | 当前修理任务 | 已经做出，但还没完整检查 | 使用者看不到明确的产品入口，部署边界与所有者预期不一致。 | W1A@f0d4b7ca8e96 | — | 保留 frontend_config、local_first_http 回归；每次发布从前端地址进入并检查地址栏、后端根响应和 GitHub main 同时包含 frontend/、backend/。 |
| LOCAL-FRONTEND-ENTRY-20260917 | 已完成前端被错误地以本地文件方式展示 | 原来说好的没做到 | 当前修理任务 | 已经做出，但还没完整检查 | 项目所有者无法看到正常运行并连接 API 的既有前端，误以为前端被丢失或替换成后端。 | W1A@f0d4b7ca8e96 | — | 本地展示前必须检查 5173 与 18768 均在监听、当前选中页签 URL 为 5173、标题为病历不归零·内测版；禁止交付 file:// 或 18768 地址。 |
| FULL-FRONTEND-NOT-MERGED-20260917 | 完整 elder-ui 前端未合入当前 5173 入口 | 原来说好的没做到 | 当前修理任务 | 已经做出，但还没完整检查 | 使用者看不到此前完整界面，完整前端也无法实际读写当前后端能力 | W1A@f0d4b7ca8e96 | — | 发布前固定检查 5173 的完整品牌首页、三项底部导航、静态资源版本和浏览器持久化；禁止只凭后端健康页或旧 Service Worker 缓存判定前端完成。 |
| ELDER-SECOND-PASSWORD-20260917 | 老人端被要求重复输入网站访问密码 | 原来说好的没做到 | 当前修理任务 | 已经做出，但还没完整检查 | 真实老人用户会困惑、受阻或放弃使用，破坏完整内测体验 | W1B | — | 保留前端无第二密码合同测试、令牌/Cookie/来源/CSRF 回归和刷新后单口令真实浏览器验收。 |
