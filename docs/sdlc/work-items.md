# 工作账本

> 由 `.dz/state.json` 生成，请不要在这里维护第二套状态。

| ID | 必须完成 | 所属阶段 | 当前情况 | 要做的事 | 检查标准 | 证据 | 备注 |
|---|---|---|---|---|---|---|---|
| W1 | yes | 动手制作 | implemented_unverified | 云端骨架与访问保护 | 电脑关机后目标手机仍可打开页面；未授权浏览器不能使用 AI 或备份接口；现有 DeepWe 地址和版本不变化。覆盖 R1、R9、R12。 | E-W1-LOCAL-20260916 | 云端骨架、访问保护、Linux 发布包已实现且本地测试通过；待正式 veFaaS 部署、手机访问及 DeepWe 不变性验收。 |
| W2 | yes | 动手制作 | implemented_unverified | 本地加密数据与结构升级 | 重启后数据仍可解锁；未解锁页面无法读取正文；结构升级失败不破坏旧版本；旧 SQLite 文件未被覆盖或删除。覆盖 R2、R5、R6、R8、R9、R11。 | E-W2-LOCAL-20260916 | 设备端加密仓库、锁定、重载与备份恢复已实现并本地验证；待结构升级失败场景与正式设备验收。 |
| W3 | yes | 动手制作 | implemented_unverified | 原件优先、AI 同意与真实媒体 | 关闭 AI 仍能完整记录；只有显式动作产生外部调用；真实浏览器媒体不丢原件并完成受控闭环。覆盖 R2、R3、R4、R10、R11、R12。 | E-W3-LOCAL-20260916 | 原件优先、外发同意、媒体临时处理和失败保留已实现；待真实手机媒体与付费模型受控验收。 |
| W4 | yes | 动手制作 | implemented_unverified | 时间线、历史、删除和就诊材料 | 至少六条合成或打码记录可按全部维度查找；专用测试记录删除后本地正文和原件不可读；PDF 不经过云端生成。覆盖 R4、R5、R6、R7、R9、R11、R12。 | E-W4-LOCAL-20260916 | 时间线、筛选、历史、删除和浏览器打印材料已实现；待六条记录、真实手机及 PDF 全流程验收。 |
| W5 | yes | 动手制作 | implemented_unverified | TOS 密文备份、恢复和反馈 | TOS 对象没有健康明文、API 密钥或恢复密钥；全新浏览器可恢复并核对记录和媒体；损坏备份不覆盖当前数据。覆盖 R6、R8、R9、R13。 | E-W5-LOCAL-20260916 | 加密备份、隔离预览恢复、TOS 短时授权和本地反馈已实现；待私有 TOS 与全新浏览器实测。 |
| W6 | yes | 实际检查 | pending | 全量验收、文档和主干发布 | R1-R13 均有当前提交和实际部署上的可复现证据；无高严重度未解决问题；所有者可从手机完成录入、核对、备份和恢复。 | — | 等待云端部署、真实 AI/手机/跨浏览器验收后再更新 README、提交并覆盖 GitHub main。 |
| D1 | yes | 把做法说清楚 | implemented_unverified | 本地优先产品与系统设计 | 已确认的设计完整定义页面结构、设备本地加密数据、veFaaS 无状态职责、TOS 密文备份、安全边界、失败恢复和 W1-W6 验收。覆盖 R1-R13。 | E-D1-PLAN | 已确认规格与方案作为设计证据。 |
| W2@bb2619691d27 | yes | 动手制作 | implemented_unverified | 本地加密数据与结构升级 | 重启后数据仍可解锁；未解锁页面无法读取正文；结构升级失败不破坏旧版本；旧 SQLite 文件未被覆盖或删除。覆盖 R2、R5、R6、R8、R9、R11。 | — | 设备端加密仓库、锁定、重载与备份恢复已实现并本地验证；待结构升级失败场景与正式设备验收。<br>keep from W2: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required |
| W3@bb2619691d27 | yes | 动手制作 | implemented_unverified | 原件优先、AI 同意与真实媒体 | 关闭 AI 仍能完整记录；只有显式动作产生外部调用；真实浏览器媒体不丢原件并完成受控闭环。覆盖 R2、R3、R4、R10、R11、R12。 | — | 原件优先、外发同意、媒体临时处理和失败保留已实现；待真实手机媒体与付费模型受控验收。<br>keep from W3: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required |
| W4@bb2619691d27 | yes | 动手制作 | implemented_unverified | 时间线、历史、删除和就诊材料 | 至少六条合成或打码记录可按全部维度查找；专用测试记录删除后本地正文和原件不可读；PDF 不经过云端生成。覆盖 R4、R5、R6、R7、R9、R11、R12。 | — | 时间线、筛选、历史、删除和浏览器打印材料已实现；待六条记录、真实手机及 PDF 全流程验收。<br>keep from W4: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required |
| W5@bb2619691d27 | yes | 动手制作 | implemented_unverified | TOS 密文备份、恢复和反馈 | TOS 对象没有健康明文、API 密钥或恢复密钥；全新浏览器可恢复并核对记录和媒体；损坏备份不覆盖当前数据。覆盖 R6、R8、R9、R13。 | — | 加密备份、隔离预览恢复、TOS 短时授权和本地反馈已实现；待私有 TOS 与全新浏览器实测。<br>keep from W5: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required |
| W1@bb2619691d27 | yes | 动手制作 | implemented_unverified | 云端骨架与访问保护 | 本地打开前端 5173 显示完整产品页面并能安全调用后端 18768；后端不托管前端；未授权来源不能使用 AI 或备份接口；线上只分享前端地址且现有 DeepWe 地址和版本不变化。覆盖 R1、R9、R12。 | E-W1-SPLIT-20260916 | 本地双端口、访问保护和独立 Linux 后端包已实现并通过；正式云端双服务、手机和 DeepWe 不变性仍待验收。 |
| W6@bb2619691d27 | yes | 实际检查 | pending | 全量验收、文档和主干发布 | R1-R13 均有当前提交和双服务实际部署上的可复现证据；无高严重度未解决问题；所有者能从前端地址在手机完成录入、核对、备份和恢复；frontend 与 backend 一起进入 GitHub main。 | — | 等待云端部署、真实 AI/手机/跨浏览器验收后再更新 README、提交并覆盖 GitHub main。<br>revise from W6: 发布候选必须同时包含并验证独立前端服务和独立后端服务。; fresh checks required |
| D1@bb2619691d27 | yes | 把做法说清楚 | implemented_unverified | 本地优先产品与系统设计 | 已确认的设计完整定义单仓库双服务、前端唯一用户入口、独立 API 地址、精确 CORS/会话、本地加密数据、TOS 密文备份、安全边界和验收方式。覆盖 R1-R13。 | E-D1-SPLIT-20260916 | 前后端分离规格与方案已确认并形成当前设计证据。 |
| W1A | yes | 动手制作 | implemented_unverified | 修复前端入口与后端服务混淆 | 从前端 5173 打开完整产品页面，页面安全调用后端 18768；后端根地址不托管前端；README 只把前端地址写成使用入口。 | E-W1A-SPLIT-20260916 | 真实浏览器和自动化回归证明前端入口、独立 API、跨端口登录和 README 修复成立。 |
| W1@bb2619691d27@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | 云端骨架与访问保护 | 本地打开前端 5173 显示完整产品页面并能安全调用后端 18768；后端不托管前端；未授权来源不能使用 AI 或备份接口；线上只分享前端地址且现有 DeepWe 地址和版本不变化。覆盖 R1、R9、R12。 | — | 本地双端口、访问保护和独立 Linux 后端包已实现并通过；正式云端双服务、手机和 DeepWe 不变性仍待验收。<br>keep from W1@bb2619691d27: 仅将已经确认的前后端分离规格和方案登记为去除文末空白的版本；产品承诺、代码实现和验收口径完全不变，保留现有实现但重新绑定当前合同并要求新证据。; fresh checks required |
| W2@bb2619691d27@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | 本地加密数据与结构升级 | 重启后数据仍可解锁；未解锁页面无法读取正文；结构升级失败不破坏旧版本；旧 SQLite 文件未被覆盖或删除。覆盖 R2、R5、R6、R8、R9、R11。 | — | 设备端加密仓库、锁定、重载与备份恢复已实现并本地验证；待结构升级失败场景与正式设备验收。<br>keep from W2: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required<br>keep from W2@bb2619691d27: 仅将已经确认的前后端分离规格和方案登记为去除文末空白的版本；产品承诺、代码实现和验收口径完全不变，保留现有实现但重新绑定当前合同并要求新证据。; fresh checks required |
| W3@bb2619691d27@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | 原件优先、AI 同意与真实媒体 | 关闭 AI 仍能完整记录；只有显式动作产生外部调用；真实浏览器媒体不丢原件并完成受控闭环。覆盖 R2、R3、R4、R10、R11、R12。 | — | 原件优先、外发同意、媒体临时处理和失败保留已实现；待真实手机媒体与付费模型受控验收。<br>keep from W3: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required<br>keep from W3@bb2619691d27: 仅将已经确认的前后端分离规格和方案登记为去除文末空白的版本；产品承诺、代码实现和验收口径完全不变，保留现有实现但重新绑定当前合同并要求新证据。; fresh checks required |
| W4@bb2619691d27@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | 时间线、历史、删除和就诊材料 | 至少六条合成或打码记录可按全部维度查找；专用测试记录删除后本地正文和原件不可读；PDF 不经过云端生成。覆盖 R4、R5、R6、R7、R9、R11、R12。 | — | 时间线、筛选、历史、删除和浏览器打印材料已实现；待六条记录、真实手机及 PDF 全流程验收。<br>keep from W4: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required<br>keep from W4@bb2619691d27: 仅将已经确认的前后端分离规格和方案登记为去除文末空白的版本；产品承诺、代码实现和验收口径完全不变，保留现有实现但重新绑定当前合同并要求新证据。; fresh checks required |
| W5@bb2619691d27@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | TOS 密文备份、恢复和反馈 | TOS 对象没有健康明文、API 密钥或恢复密钥；全新浏览器可恢复并核对记录和媒体；损坏备份不覆盖当前数据。覆盖 R6、R8、R9、R13。 | — | 加密备份、隔离预览恢复、TOS 短时授权和本地反馈已实现；待私有 TOS 与全新浏览器实测。<br>keep from W5: 本地加密、原件优先、时间线、备份和反馈不依赖前后端是否同源，现有实现继续保留并重新验证。; fresh checks required<br>keep from W5@bb2619691d27: 仅将已经确认的前后端分离规格和方案登记为去除文末空白的版本；产品承诺、代码实现和验收口径完全不变，保留现有实现但重新绑定当前合同并要求新证据。; fresh checks required |
| W6@bb2619691d27@f0d4b7ca8e96 | yes | 实际检查 | pending | 全量验收、文档和主干发布 | R1-R13 均有当前提交和双服务实际部署上的可复现证据；无高严重度未解决问题；所有者能从前端地址在手机完成录入、核对、备份和恢复；frontend 与 backend 一起进入 GitHub main。 | — | GitHub main 已更新到 4ecbd92，确认同时包含 frontend/ 与 backend/；仍待新建双云服务、配置私有 TOS、真实线上浏览器和手机验收。 |
| D1@bb2619691d27@f0d4b7ca8e96 | yes | 把做法说清楚 | implemented_unverified | 本地优先产品与系统设计 | 已确认的设计完整定义单仓库双服务、前端唯一用户入口、独立 API 地址、精确 CORS/会话、本地加密数据、TOS 密文备份、安全边界和验收方式。覆盖 R1-R13。 | E-D1-COMMIT-20260916, E-D1-INAPP-20260917 | 前后端分离 v2 设计保持有效；当前目标切换为 Codex 内置浏览器现场复核。 |
| W1A@f0d4b7ca8e96 | yes | 动手制作 | implemented_unverified | 修复前端入口与后端服务混淆 | 从前端 5173 打开完整产品页面，页面安全调用后端 18768；后端根地址不托管前端；README 只把前端地址写成使用入口。 | E-W1A-COMMIT-20260916, E-LOCAL-FRONTEND-ENTRY-20260917, E-W1A-FULL-20260917, E-W1A-FULL-FF62D82 | 提交 ff62d82 已在 5173 实测；36 项前端、765 项 Python、合同检查、刷新持久化和 5173→18768 登录闭环通过。 |
| W1B | yes | 动手制作 | pending | 移除老人端第二密码并建立设备绑定 | 老人端无网站密码框；家属短时链接可绑定设备；持久设备会话可跨刷新复用；失效只提示家属且不阻断本地功能；来源与 CSRF 防护保持有效；浏览器完成真实操作验证 | — | — |
